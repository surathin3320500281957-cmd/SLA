# SLA Dashboard — สัญญายังไม่ส่งมอบในระบบ

Dashboard ติดตามสถานะดำเนินงานสัญญาที่ยังไม่ส่งมอบ ดึงข้อมูลจากไฟล์ Excel (Google Drive) แล้ว build เป็นไฟล์ `index.html` แบบ static ไม่มี backend, ไม่มี database — ข้อมูลทั้งหมด hardcode ไว้ในไฟล์ HTML ตอน build แต่ละครั้ง

**Deploy:** อัปโหลดไฟล์ HTML ทับ `index.html` ใน GitHub repository นี้ แล้ว GitHub Pages จะ deploy ให้อัตโนมัติ

**⚠️ ห้ามอัปโหลดไฟล์ Excel ต้นฉบับขึ้น repo นี้เด็ดขาด** เพราะมีข้อมูลลูกค้า (ชื่อ, รหัสลูกค้า, ยอดหนี้) — ใช้ไฟล์ Excel แค่เป็น input สำหรับ Claude นำไป build ข้อมูลลง `index.html` เท่านั้น แล้วอัปโหลดเฉพาะ `index.html` ที่ build เสร็จแล้วขึ้น repo

---

## โครงสร้าง Dashboard

| Tab | ข้อมูลจาก | จำนวนที่แสดงตอนนี้ |
|---|---|---|
| **ภาพรวม** | sheet `AF` ทั้งหมด | ~1,000 รายการ |
| **ไม่ดำเนินการ(สคล/สคจ ชี้แจง)** | sheet `สำหรับกรอง-ตรวจสอบ` join กับ `AF` | ~130 รายการ |
| **ไม่ดำเนินการ(ข้อมูลหน่วยงานอื่น)** | sheet `รายเดือน-ชน` join กับ `AF` | ~140 รายการ |

---

## Sheet ที่ใช้ใน Excel ต้นทาง

ไฟล์ Excel: `สัญญายังไม่ส่งมอบในระบบ-SLA.xlsx`

| Sheet | ใช้ทำอะไร |
|---|---|
| `AF` | ข้อมูลหลัก (master) — ทุกสัญญาทั้งหมด รวม status, วันที่แต่ละขั้นตอนซ่อม, หนี้ค้าง |
| `สำหรับกรอง-ตรวจสอบ` | รายการ "ไม่ดำเนินการ" ที่เจ้าหน้าที่ชี้แจงเหตุผลไว้ (`ปรับสรุปการUPDATE`) |
| `รายเดือน-ชน` | รายการไม่ดำเนินการจากหน่วยงานอื่น (ข้อมูลย้อนหลัง) พร้อมชี้แจงเหตุผลเช่นกัน |

---

## Column Mapping สำคัญ

### จาก sheet `AF` (header อยู่ row 3 ในไฟล์ / index 2 แบบ 0-based)

| Field ใน Dashboard | Column ใน Excel | หมายเหตุ |
|---|---|---|
| `dept` | `DEPT_SHORT_NAME` | กอง |
| `bp` | `BP_SHORT_NAME` | สาขา |
| `proj` | `PROJ_NAME` | โครงการ |
| `house` | `HOUSE_NO_ACT` | บ้านเลขที่ (**ไม่ใช่** `HOUSE_NO_STD`) |
| `cust` | `CUST_CODE` | รหัสลูกค้า — ต้อง normalize (ตัด `.0` ท้าย float ออก) ก่อน join |
| `status` / `next_custcare` | `NEXT_CUSTCARE` | สถานะซ่อม เช่น "ไม่ดำเนินการ", "รออนุมัติงบ" ฯลฯ |
| `update` (non-NA rows) | `CUSTCARE_STATUS_NAME` | ใช้เฉพาะ record ที่ status ≠ "ไม่ดำเนินการ" |
| `guarantee` | `ST_GUARANTEE` | การค้ำประกัน |
| `confirm_flag` | `CONFIRM_FLAG` | ซ่อม / ไม่ดำเนินการ |
| `start_con` | `เริ่มสัญญา` | ค.ศ. → ต้อง +543 |
| `due_last` | `Dueล่าสุด` | ค.ศ. → ต้อง +543 |
| `due_amt` | `เงินDue` | |
| `overdue` | `งวดค้าง` | |
| `debt` | `หนี้ค้าง` | ใช้เป็น flag สำหรับ filter "มีหนี้ค้าง/ไม่มีหนี้ค้าง" |
| `d01`–`d11` | `DATE_01`–`DATE_11` | วันที่แต่ละขั้นตอนซ่อม (datetime string, +543) |
| `aging09` | `AGING_W_09` | วันรอจัดหาผู้รับจ้าง |
| `ck_aging09` | `CK_AGING_09` | ช่วงวันรอจัดหาผู้รับจ้าง (เกิน 13 วัน / ภายในกำหนด) |
| `days_lag` | คำนวณจาก `MAX_STATUS_DATE` | = (วันที่ update ไฟล์) − (MAX_STATUS_DATE) หน่วยเป็นวัน |

**⚠️ ข้อควรระวังเรื่องปี:**
- `เริ่มสัญญา`, `Dueล่าสุด`, `DATE_01-11`, `MAX_STATUS_DATE` → เก็บเป็น **ค.ศ.** ต้อง `+543`
- `สิ้นสุดสัญญา` → column นี้ถูก**ลบออกจาก Dashboard แล้ว** (ผู้ใช้ขอให้เอาออกเพราะข้อมูลสับสน) แต่ field ยังมีอยู่ใน data structure เผื่ออนาคต

### จาก sheet `สำหรับกรอง-ตรวจสอบ` (data เริ่ม row 4 / index 3 เป็นต้นไป)

| Field | Column index (0-based) | ใช้ทำอะไร |
|---|---|---|
| `dept` | col 2 | กอง |
| `bp` | col 3 | สาขา |
| `proj` | col 4 | โครงการ |
| `house` | col 5 | บ้านเลขที่ |
| `cust` | col 6 | รหัสลูกค้า — key สำหรับ join กับ AF |
| `cust_name` | col 7 | ชื่อลูกค้า |
| `change_status` | col 11 | CHANGE_STATUS |
| `guarantee` | col 13 | การค้ำประกัน |
| `confirm_flag` | col 14 | |
| `update` | **col 21** | **`ปรับสรุปการUPDATE`** ← เหตุผลที่ยังไม่ดำเนินการ (คำชี้แจง) |

**สำคัญ:** column 21 (`ปรับสรุปการUPDATE`) เป็นค่าที่ถูกต้องเท่านั้น — **ห้ามใช้** col 17 (`สรุปการUPDATE`) เพราะมีค่าว่างปนอยู่และไม่ตรงกับความเป็นจริง

**ระวัง:** บาง row ใน sheet นี้อาจมีความยาว (`len(r)`) น้อยกว่า 22 หรือเป็น row ว่างล้วน ต้อง guard ด้วย `if len(r) < 22 or not r[2]: continue` ก่อนอ่าน column 21 เสมอ ไม่งั้นจะ error `IndexError: tuple index out of range`

### จาก sheet `รายเดือน-ชน` (header อยู่ row 61 / index 60, data เริ่ม row 62 / index 61)

| Field | Column index | หมายเหตุ |
|---|---|---|
| `dept` | col 2 | |
| `bp` | col 3 | |
| `proj` | col 4 | |
| `house` | col 5 | |
| `cust` | col 6 | key join กับ AF |
| `update` | **col 18** | `ปรับสรุปการUPDATE` (เหมือน logic เดียวกับ สำหรับกรอง-ตรวจสอบ) |

---

## Business Logic สำคัญ (อย่าลืม!)

1. **`ปรับสรุปการUPDATE` ไม่ใช่สถานะการซ่อม** — เป็นแค่ **คำชี้แจงเหตุผล** ว่าทำไมเคสถึงยังค้างอยู่ (เช่น "ยังไม่สำรวจ", "สำรวจแล้วรอลงระบบ SLA", "อยู่ในค้ำประกันฝ่ายก่อสร้าง") แม้จะมีคำว่า "ซ่อมแล้ว" ปนอยู่ ไม่ได้แปลว่ากระบวนการซ่อมเริ่มแล้วจริง

2. **การยืนยันว่าซ่อมเริ่มจริงหรือยัง** ให้ดูที่ `DATE_01`–`DATE_11` ใน sheet `AF` เท่านั้น — ถ้ามีวันที่ปรากฏ = เริ่มกระบวนการซ่อมแล้วจริง ถ้าว่างทั้งหมด = ยังไม่เริ่มอะไรเลย

3. **`รอการUpdate` (ค่า default ของระบบ)** จะปรากฏเมื่อ cust code ใน `AF` (status = "ไม่ดำเนินการ") **หาไม่เจอ** ใน sheet `สำหรับกรอง-ตรวจสอบ` — แปลว่าเจ้าหน้าที่ยังไม่ได้กรอกคำชี้แจงลงไฟล์ ไม่ใช่ bug ของ Dashboard

4. **จำนวนรายการในแต่ละ tab จะเปลี่ยนทุกครั้งที่ไฟล์ Excel อัปเดต** เพราะสถานะขยับตลอด (เคสเก่าปิด/ส่งมอบไปแล้วหลุดออก, เคสใหม่เข้ามาเพิ่ม)

5. **บางครั้งพบค่าประหลาด เช่น `'X'`** ใน `ปรับสรุปการUPDATE` (data entry ผิดพลาดจากผู้กรอก) — ให้ normalize เป็น `'รอการUpdate'` ก่อน inject เสมอ

---

## UC / MC (สี badge สำหรับแต่ละสถานะ)

`UC`/`UB` และ `MC`/`MB` เป็น object เก็บสี/class badge ของแต่ละค่าใน `ปรับสรุปการUPDATE`

**⚠️ ทุกครั้งที่ update ข้อมูล ต้องตรวจสอบว่า:**
```
set(ค่า update ทั้งหมดใน NA/MN) − set(keys ใน UC/MC) == empty set
```
ถ้าเจอ key ใหม่ที่ไม่มีใน UC/MC (เช่น พบคำใหม่ "รอการจัดจ้าง") ต้อง**เพิ่มเข้า UC/UB (สำหรับ NA) หรือ MC/MB (สำหรับ MN)** ก่อน inject ข้อมูล ไม่งั้น column badge จะไม่มีสี/ไม่ถูกนับในตารางสรุป

---

## Filter ที่มีในแต่ละ Tab

- กอง / สาขา / โครงการ (dropdown ลดหลั่นกัน)
- ปรับสรุปการ UPDATE
- ช่วงวันรอจัดหาผู้รับจ้าง (`ck_aging09`)
- หนี้ค้าง (มีหนี้ค้าง / ไม่มีหนี้ค้าง) — ใช้ field `debt`

Tab ภาพรวม มีเพิ่ม: การค้ำประกัน, การซ่อม, สถานะซ่อม

---

## ขั้นตอนการ Update ข้อมูล (สำหรับ Claude ในแชทใหม่)

1. รับไฟล์ Excel ใหม่จากผู้ใช้ — **ทำงานกับไฟล์ในเครื่อง (sandbox) เท่านั้น อย่าอัปโหลดไฟล์ Excel ขึ้น GitHub เด็ดขาด**
2. **ตรวจสอบก่อนเสมอ** ว่า 6 columns หนี้ค้างมีอยู่ใน sheet `AF` หรือไม่ (`เริ่มสัญญา`, `สิ้นสุดสัญญา`, `Dueล่าสุด`, `เงินDue`, `งวดค้าง`, `หนี้ค้าง`) — ถ้าไม่มี ให้แจ้งผู้ใช้ก่อน อย่า build ทับแบบเงียบๆ
3. Build 3 datasets: `AF_new` (ทุก row จาก AF), `NA_data` (จาก สำหรับกรอง-ตรวจสอบ join AF), `MN_new` (จาก รายเดือน-ชน join AF)
4. หา `UPDATE_DATE` อัตโนมัติจากค่า `MAX_STATUS_DATE` ที่มากที่สุดใน sheet AF (ไม่ต้องถามผู้ใช้)
5. Normalize คำว่า `'X'` หรือค่าประหลาดอื่นๆ ใน `update` field ให้เป็น `'รอการUpdate'`
6. ตรวจสอบ `UC`/`MC` ว่าครอบคลุมทุกค่าที่เจอใน `update` field — ถ้าขาด เพิ่มเข้าไปก่อน inject
7. Inject 3 arrays เข้า `<script>` ด้วย regex replace (`const AF=`, `const NA=`, `const MN=`), อัปเดต tab pill (`tb-ov`, `tb-na`, `tb-mn`) และ timestamp
8. รัน `node --check` ตรวจ syntax ก่อนส่งไฟล์
9. ตรวจสอบครั้งสุดท้าย: จำนวนแต่ละ tab ตรงกับ pill, ไม่มี `รอการUpdate` ค้าง (ถ้ามีให้แจ้งผู้ใช้), debt/days_lag มีข้อมูล
10. ส่งไฟล์ `index.html` ที่ build เสร็จแล้วให้ผู้ใช้ **นำไปอัปโหลดเองบน GitHub** — Claude ไม่มีสิทธิ์เข้าถึง GitHub repo โดยตรง

---

## ข้อควรระวังอื่นๆ

- **Browser cache**: หลัง deploy ขึ้น GitHub แล้ว ให้ผู้ใช้ hard refresh (Ctrl+Shift+R / Cmd+Shift+R) เพราะ browser มักโชว์เวอร์ชันเก่าค้างไว้
- **cust code format**: ใน Excel บาง column เก็บเป็น float string (`"200295872.0"`) บางที่เป็น int string (`"200295872"`) — ต้อง normalize ด้วยฟังก์ชัน `nc()` ก่อน join เสมอ ไม่งั้นจะ join ไม่เจอและกลายเป็น `รอการUpdate` ผิดๆ
- **ไม่มีระบบ backup อัตโนมัติ** — ถ้าต้องการย้อนกลับเวอร์ชันเก่า ต้องพึ่ง Git history บน GitHub repository เอง (Claude ไม่ได้เก็บ version history ของไฟล์ที่ build ไว้ข้ามแชท)
- **ไฟล์ใหญ่ (Excel) อัปโหลดขึ้น GitHub ไม่ได้ผ่านหน้าเว็บปกติเมื่อเกิน limit การ preview** และการลบไฟล์/repo อาจติดปัญหา **GitHub sudo mode** (ต้องยืนยันตัวตนซ้ำผ่านอีเมล) โดยเฉพาะบน mobile browser — ถ้าเจอปัญหานี้ แนะนำให้ลองบนคอมพิวเตอร์แทน หรือใช้ GitHub Desktop App
- **Repo เก่าที่มีไฟล์ Excel หลุดขึ้นไปเคยเกิดขึ้นมาแล้ว** — ถูกลบทิ้งไปแล้ว repo ปัจจุบันสะอาด ไม่มีไฟล์ Excel ค้างอยู่ ให้ระวังไม่ให้เกิดซ้ำในอนาคต
