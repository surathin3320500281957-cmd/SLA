# SLA Dashboard — README

เอกสารสรุปโครงสร้าง ข้อมูล และ logic ทั้งหมดของ `index.html` เพื่อให้แชทครั้งต่อไป (ไม่ว่าจะอัปเดตข้อมูลใหม่ หรือแก้ไขเพิ่มเติม) เข้าใจภาพรวมได้เร็วขึ้น ไม่ต้องไล่หาทีละจุดใหม่ทุกครั้ง

---

## 1. ภาพรวมไฟล์และการ mapping กับ sheet ต้นฉบับ (สำคัญที่สุด — อ่านก่อนแก้อะไร)

ไฟล์เดียว `index.html` — HTML + CSS + JavaScript (Chart.js) ไม่มี backend, ข้อมูลทั้งหมด**ฝังอยู่ในไฟล์เป็น JSON literal** (3 ตัวแปรหลัก: `AF`, `NA`, `MN`)

**แต่ละแท็บมีที่มาจากคนละ sheet ใน Excel ต้นฉบับ — ⚠️ NA และ MN ไม่ได้ derive จาก AF:**

| แท็บ | ตัวแปรใน JS | ที่มาจริง | ความสัมพันธ์กับ AF |
|---|---|---|---|
| 1. ภาพรวม | `AF` | Sheet **AF** (116 คอลัมน์) | เป็น array หลัก |
| 2. ไม่ดำเนินการ(ภาพรวม) | `OVNA` | `= AF.filter(status==='ไม่ดำเนินการ')` | **derived จาก AF จริง** — รับประกัน 100% ว่า `update` ไม่มีวันอยู่ใน 11 สถานะเลย |
| 3. ไม่ดำเนินการ(สคล/สคจ ชี้แจง) | `NA` | Sheet **สำหรับกรอง-ตรวจสอบ** (163 แถว) | **array แยก, ไม่ใช่ filter จาก AF** — ดู 1.1 |
| 4. ไม่ดำเนินการ(ข้อมูลหน่วยงานอื่น) | `MN` | Sheet **รายเดือน-ชน** (151 แถว) | **array แยก** — ดู 1.1 |

### 1.1 วิธีสร้าง NA และ MN (สำคัญ — ต้องทำแบบนี้ทุกครั้งที่อัปเดตข้อมูล)

**ทั้งคู่ยึดจำนวนแถว/รายชื่อลูกค้าจาก sheet ต้นทางของตัวเอง ไม่ใช่ filter จาก AF:**

```
NA:  วนทุกแถวใน "สำหรับกรอง-ตรวจสอบ"
     → ถ้ารหัสลูกค้าพบใน AF: copy record ทุกฟิลด์จาก AF ตรงๆ
     → ถ้าไม่พบใน AF (ลูกค้าหลุดออกจากระบบไปแล้ว เช่น ส่งมอบแล้ว):
       ใช้ฟิลด์พื้นฐานจาก "สำหรับกรอง-ตรวจสอบ" เอง (dept/bp/proj/house/cust_name/
       change_status/confirm_flag), update = ปรับสรุปการUPDATE, ฟิลด์อื่นเว้นว่าง

MN:  วนทุกแถวใน "รายเดือน-ชน" ด้วยหลักการเดียวกัน (ใช้ "รหัสลูกค้า" คอลัมน์กับ
     "ปรับสรุปการUPDATE" คอลัมน์แทน) — ค่า guarantee คงที่ = "หลังค้ำประกัน" สำหรับ
     กลุ่มที่ไม่พบใน AF
```

⚠️ **ผลที่ตามมา:** เพราะ "สำหรับกรอง-ตรวจสอบ"/"รายเดือน-ชน" เป็น list ที่สาขากรอกเอง อาจ**ล้าหลัง**กว่า AF จริง — บางคนใน list นี้อาจ**ขยับไปสถานะซ่อมจริงใน AF แล้ว** (progress ไปแล้วแต่ยังไม่ได้ลบออกจาก list ชี้แจง) ทำให้ **NA/MN มี record ที่ `update` อยู่ใน 11 สถานะจริงปนอยู่ด้วย** (ไม่ใช่แค่ "ไม่ดำเนินการ" ล้วนๆ เหมือน OVNA) — ตรวจสอบล่าสุด (18-08-69): NA มี 39/163, MN มี 36/151 ที่ progress ไปแล้ว

**สรุปสั้นๆ:** OVNA เท่านั้นที่รับประกันว่าไม่มีสถานะจริงปนอยู่ (เพราะ derive จาก AF ตรงๆ) — **NA และ MN ต้องแก้ไขด้วย logic เดียวกับ AF ทุกจุดที่เกี่ยวกับสถานะซ่อม** (ดูหัวข้อ 4)

**ขนาดข้อมูล ณ ชุดล่าสุด (18-08-69):** AF 1,021 | OVNA 143 | NA 163 | MN 151

---

## 2. โครงสร้างฟิลด์สำคัญใน `AF` / `NA` / `MN` (record หนึ่งแถว)

```
dept, bp, proj, house, cust, cust_name, change_status,
status,          // = ค่า NEXT_CUSTCARE ดิบจาก AF sheet (ไม่ใช่ curStage-grouped)
update,          // "สถานะซ่อมปัจจุบัน" — coalesce: CUSTCARE_STATUS_NAME ถ้าไม่ว่าง,
                 // ไม่งั้น lookup "ปรับสรุปการUPDATE" จาก สำหรับกรอง-ตรวจสอบ,
                 // ไม่งั้น fallback เป็น NEXT_CUSTCARE (ดูหัวข้อ 6 ขั้นตอนที่ 3)
guarantee, confirm_flag,
next_custcare,   // ⚠️ เลิกใช้แล้ว (เหมือน status ทุกประการ) ไม่ผูกกับระบบใดๆ
start_con,       // วันทำสัญญา จาก CONTRACT_DATE (datetime, ปีเก็บเป็น พ.ศ. ตรงๆ)
due_last,        // จาก Dueล่าสุด (ตัวเลข YYYYMMDD, ปีเป็น ค.ศ. ต้อง +543)
due_amt,         // จาก เงินDue, format comma 2 ตำแหน่ง เช่น "1,452.70"
overdue,         // จาก งวดค้าง
debt,            // จาก หนี้ค้าง, format comma
days_lag,        // ⚠️ ข้อมูลดิบมีปัญหา (ดูหัวข้อ 3) ห้ามใช้ตรงๆ ต้องผ่าน statusLagDays()
ck_custcare_st,      // ข้อความดิบจาก CK_CUSTCARE_ST อาจมีวันที่ 8 หลักนำหน้า
ck_custcare_st_grp,  // = ck_custcare_st ตัดวันที่ออก, หรือ "ไม่เคยซ่อม" ถ้าว่าง
d01..d11,        // จาก DATE_01..DATE_11 (datetime, ปีเป็น ค.ศ. ต้อง +543 ถ้า<2100)
                 // ⚠️ มีปัญหาล้ำหน้า 1 สถานะ (ดูหัวข้อ 3) ห้ามอ่านตรงๆ ต้องผ่าน dCell()
aging09, ck_aging09,
contract_type,   // จาก CONTRACT_TYPE1 — มีอยู่เฉพาะใน sheet AF เท่านั้น (ดูหัวข้อ 5)
note             // "หมายเหตุ" — join รหัสลูกค้ากับ sheet "สำหรับกรอง-ตรวจสอบ" (คอลัมน์ท้ายสุดของ sheet)
                 // ⚠️ เพิ่มเข้า AF โดยตรง (ไม่ใช่แค่ NA) เพื่อให้ OVNA (=AF.filter(...)) ได้คอลัมน์นี้ด้วยอัตโนมัติ
                 // คนที่ไม่พบใน "สำหรับกรอง-ตรวจสอบ" (ส่วนใหญ่ของ AF ที่กำลังซ่อมอยู่จริง) จะได้ note='' แสดง "—"
```

`cust` = รหัสลูกค้า = `CUST_CODE`/`รหัสลูกค้า` ใน Excel ทุก sheet (ยืนยันแล้วเป็นค่าเดียวกัน ใช้ join ข้าม sheet ได้เสมอ)

### STAGES11 (ลำดับสถานะซ่อมมาตรฐาน 11 ขั้น — ต้องเรียงลำดับนี้เสมอ คู่กับ d01-d11)
```
1. รับคำร้องแจ้งซ่อม   4. ประมาณราคา            7. รอการจัดจ้าง        10. ดำเนินการเสร็จสิ้น
2. สำรวจซ่อม           5. ตรวจสอบและรับรองราคา  8. จัดหาผู้รับจ้าง      11. ขอเบิกเงินค่าจ้าง
3. ยืนยันการสำรวจซ่อม   6. อนุมัติสรุปค่าก่อสร้าง 9. ดำเนินการซ่อม
```
`STAGES12 = [...STAGES11, 'ไม่ดำเนินการ']`

---

## 3. ⚠️ ลักษณะพิเศษของข้อมูลดิบ — "รูปแบบล้ำหน้า 1 สถานะ" (พบ 4 จุด ทุกจุดแก้ไปแล้ว)

ระบบต้นทาง (AF sheet) มี pattern เดียวกันซ้ำๆ: **บันทึกวันที่/ข้อมูลของสถานะถัดไปไว้ล่วงหน้า 1 ขั้นเสมอ** แม้ `update` (สถานะทางการ) ยังไม่ขยับไปถึง — ไม่เคยล้ำเกิน 1 ขั้น ยืนยันด้วยข้อมูลจริงทุกครั้ง

| จุดที่กระทบ | ข้อมูลดิบ | ฟังก์ชันที่แก้ |
|---|---|---|
| คอลัมน์วันที่ d01-d11 | `DATE_01..11` มีวันที่ของสถานะถัดไปติดมาด้วย | `dCell(x,n)` |
| เช็คสถานะซ่อมหลังสุด | `CK_CUSTCARE_ST` ล้ำหน้า `update` ได้ 1 ขั้น | `ckStatus(x)` |
| วันค้างสถานะ | `days_lag` (จาก `MAX_STATUS_DATE`) คำนวณจากวันที่ล้ำหน้า ต่างจากจริงได้ถึง 100+ วัน | `statusLagDays(x)` |
| วันค้างสถานะซ่อมหลังสุด | `ckLagValue()` เดิม fallback ไปใช้ `days_lag` ดิบ (บั๊กเดียวกันซ่อนอยู่) | `ckLagValue(x)` (แก้ให้ fallback ไป `statusLagDays()` แทน) |

**หลักการแก้ทั้งหมด:** ถ้า `update` อยู่ใน STAGES11 (กำลังซ่อมจริง) → ใช้ข้อมูลของ**สถานะปัจจุบันเอง**เท่านั้น (ผ่าน `stageIdx(x)`) ไม่เชื่อค่าที่ "ล้ำหน้า" ไปจากสถานะปัจจุบัน ถ้าอยู่นอก STAGES11 (กลุ่มไม่ดำเนินการ) → ใช้ค่าดิบเดิมตามปกติ (ไม่มีปัญหานี้)

---

## 4. Logic / ฟังก์ชันหลักที่แก้ไว้แล้ว (ห้ามลบ/แก้โดยไม่เข้าใจก่อน)

ทั้งหมดอยู่ในส่วน `<script>` ประมาณบรรทัด 440-640

| ฟังก์ชัน/ค่าคงที่ | หน้าที่ |
|---|---|
| `STAGES11` / `STAGES12` | ลำดับสถานะมาตรฐาน (ห้ามสลับลำดับ) |
| `curStage(x)` | คืน `x.update` ถ้าอยู่ใน STAGES11, ไม่งั้นคืน `'ไม่ดำเนินการ'` — ใช้กับ filter/กราฟ/pivot ของ "สถานะซ่อมปัจจุบัน" |
| `stageIdx(x)` | ลำดับ (1-11) ของสถานะปัจจุบัน, คืน `0` ถ้านอก STAGES11 |
| `dCell(x,n)` | คืนค่า `d0n` เฉพาะ `n <= stageIdx(x)` ไม่งั้นคืน `'—'` |
| `ckStatus(x)` | ถ้าอยู่ใน STAGES11 → คืน `x.update` (sync ตรงเป๊ะ); ถ้าไม่ → คืน `x.ck_custcare_st_grp` |
| `statusLagDays(x)` | ถ้าอยู่ใน STAGES11 → คำนวณ `UPD_REF_DATE - d0N(สถานะปัจจุบัน)`; ถ้าไม่ → คืนค่า raw `x.days_lag` |
| `updSummary(x)` | คำชี้แจง (ปรับสรุปการUPDATE) — คืนค่าเฉพาะกลุ่มไม่ดำเนินการ, กลุ่มกำลังซ่อมคืนค่าว่าง |
| `parseDMY(str)` | แปลง `"DD/MM/YY"` (พ.ศ.) เป็น JS Date |
| `contractLagDays(x)` | วันค้างทำสัญญา = `UPD_REF_DATE - parseDMY(x.start_con)` |
| `ckLagValue(x)` | วันค้างสถานะซ่อมหลังสุด — ถ้า `ck_custcare_st` มีวันที่ฝัง ใช้วันนั้น; ไม่งั้น fallback ไป `statusLagDays(x)` (**ไม่ใช่** raw `days_lag`) |
| `UPD_REF_DATE` | วันที่อ้างอิงคำนวณ "วันค้าง" ทั้งหมด — **ต้องอัปเดตทุกครั้งที่ข้อมูลใหม่** ให้ตรงกับ header "อัปเดต: ..." |

### 4.1 สิ่งที่ถูกลบออกไปแล้ว (อย่าเอากลับมา)
- Dropdown filter "สถานะซ่อมถัดไป" (`ov-next`) — ลบออกจากระบบทั้งหมดแล้ว (dead code, ไม่เคยถูกใช้กรองจริง)

### 4.2 ขอบเขตการแก้ไขปัญหาข้อ 3 — ครอบคลุมกี่แท็บ
**ครอบคลุม 3 แท็บ: ภาพรวม (AF), ไม่ดำเนินการ(สคล/สคจ) (NA), ข้อมูลหน่วยงานอื่น (MN)** — ทุกจุดที่แสดง/กรองด้วยคอลัมน์ d01-d11, เช็คสถานะซ่อมหลังสุด, วันค้างสถานะ, วันค้างสถานะซ่อมหลังสุด ใน 3 แท็บนี้ต้องเรียกผ่านฟังก์ชันในหัวข้อ 4 เท่านั้น ห้ามอ่าน field ดิบตรงๆ

**แท็บ "ไม่ดำเนินการ(ภาพรวม)" (OVNA) ไม่ต้องแก้** เพราะ `= AF.filter(status==='ไม่ดำเนินการ')` รับประกันว่า `update` ไม่มีวันอยู่ใน STAGES11 เลย ทุกฟังก์ชันในหัวข้อ 4 จะ fallback ไปใช้ค่าดิบเดิมเสมอ (no-op) — **แต่ต้องตรวจสอบสมมติฐานนี้ใหม่ทุกครั้งที่อัปเดตข้อมูล** (ดูหัวข้อ 4.3)

### 4.3 ⚠️ อย่าเชื่อ "no-op" โดยไม่ตรวจสอบ
เคยเข้าใจผิดมาก่อนว่า NA/MN เป็น no-op เหมือน OVNA (เพราะตอนนั้น NA ยัง derive จาก AF.filter จริง) — พอเปลี่ยนมาใช้ sheet ต้นทางเป็นฐาน (หัวข้อ 1.1) สมมติฐานนี้ผิดทันที ทุกครั้งที่แก้โครงสร้าง NA/MN หรืออัปเดตข้อมูลใหม่ **ให้เช็คซ้ำด้วยคำสั่งนี้ก่อนตัดสินใจว่าต้องแก้ไหม:**
```js
NA.filter(r => STAGES11.includes(r.update)).length   // ถ้า > 0 ต้องใช้ฟังก์ชันหัวข้อ 4
MN.filter(r => STAGES11.includes(r.update)).length   // ถ้า > 0 ต้องใช้ฟังก์ชันหัวข้อ 4
```

---

### 4.4 Export Excel — `{raw:true}` ป้องกันข้อมูลเพี้ยนซ้ำ
ฟังก์ชัน `exXL(id,name)` (ใช้ร่วมกันทุกปุ่ม Export Excel ทั้ง 8 จุดในทุกแท็บ) เรียก `XLSX.utils.table_to_sheet(t,{raw:true})` — **ห้ามลบ option `{raw:true}` นี้ออก** เพราะ SheetJS จะ auto-detect เนื้อหาที่ดูเหมือนวันที่/ตัวเลข (เช่น เลขบ้าน `"2/8"`) แล้วแปลงเป็นวันที่จริงในไฟล์ที่ export ออกไป ซ้ำรอยปัญหาเดียวกับที่ Google Sheets ทำตอน import (หัวข้อ 7 ข้อ 5) — เคยทดสอบยืนยันแล้วว่าไม่มี `{raw:true}` จะเพี้ยนทันทีถ้าเจอ pattern แบบนี้ในข้อมูล แม้ ณ ตอนเขียนเอกสารนี้ข้อมูลชุดปัจจุบันจะไม่มี pattern นี้ก็ตาม

⚠️ **Trade-off ที่ต้องรู้:** เพราะบังคับทุกเซลล์เป็นข้อความ คอลัมน์ตัวเลข/เงิน (due_amt, debt, วันค้างต่างๆ) ในไฟล์ Excel ที่ export ออกไปจะเป็น**ข้อความ** ไม่ใช่ตัวเลขที่ SUM/คำนวณต่อในเซลล์ Excel ได้ทันที — ยอมรับ trade-off นี้แล้วเพราะป้องกันความเสี่ยงข้อมูลเพี้ยนแบบไม่รู้ตัวซึ่งร้ายแรงกว่า

---

## 5. รหัสลูกค้า + ประเภทสัญญา + Filter

เพิ่ม 2 คอลัมน์ในตารางรายละเอียดทุกแท็บ ต่อจาก "บ้านเลขที่": **รหัสลูกค้า** (`cust`, มีอยู่แล้วทุก array) และ **ประเภทสัญญา** (`contract_type`, ใหม่)

เพราะ `CONTRACT_TYPE1` มีแค่ใน sheet AF การเติม `contract_type` ให้ `NA`/`MN` ต้อง join ย้อนกลับด้วย `cust`:
```python
ct_map = {str(cust_code): contract_type1 for แต่ละแถวใน AF sheet}
for r in AF/NA/MN: r['contract_type'] = ct_map.get(str(r['cust']), '')
```
รายการที่ join ไม่เจอ (ลูกค้าหลุดจาก AF ไปแล้ว) จะแสดง "—" — เป็นเรื่องปกติ

**Filter ที่เพิ่มในทุกแท็บ (มี dropdown + chip + logic ครบ):**
- ประเภทสัญญา (`{pfx}-ctype`)
- วันค้างทำสัญญา (`{pfx}-conlag`) — bucket 0-30/31-60/61-90/เกิน 90 วัน ผ่าน `inLagRangeStrict()` (ค่า `null` ไม่ถูกนับเข้า bucket ไหนเลย ต่างจาก `inLagRange()` เดิมที่ปนค่า null เป็น 0)

**เฉพาะแท็บภาพรวมเพิ่ม:** filter "ปรับสรุปการ UPDATE" (`ov-upd`) — กรอง exact match จาก `x.update` (6 ตัวเลือกที่อยู่นอก STAGES11) วางคอลัมน์ต่อจาก "การซ่อม" ก่อน "สถานะซ่อมปัจจุบัน" — ให้เห็นคำชี้แจงจากสาขาทันทีเมื่อกรองสถานะปัจจุบัน = "ไม่ดำเนินการ" ไม่ต้องสลับแท็บ

---

## 6. ระบบ Filter Chip (แสดง filter ที่เลือกอยู่ ลบได้ทีละตัว)

ทุกแท็บมี chip แถวใต้ filter row แสดงเฉพาะ filter ที่มีค่า สีตามหมวดหมู่ (ม่วง=กอง/สาขา/โครงการ, น้ำเงิน=ประเภทสัญญา, เขียว=สถานะซ่อม, ส้ม=วันค้าง, แดง=สถานะ KPI เฉพาะภาพรวม)

**Config หลัก:** `OV_CHIP_CFG` (ภาพรวม) และ `otherChipCfg()` (ใช้ร่วมกัน 3 แท็บที่เหลือ, สร้าง config ผ่าน `OVNA_CHIP_CFG`/`NA_CHIP_CFG`/`MN_CHIP_CFG`) — เรียก `renderChips()` ในทุกฟังก์ชัน `ren*()` โดยอัตโนมัติ ไม่ต้องแก้ทุก `onchange` handler

**เพิ่ม filter ใหม่ → ต้องเพิ่ม chip config ด้วยเสมอ** ไม่งั้น filter จะทำงานแต่ไม่มี chip แสดง

---

## 7. Workflow การอัปเดตข้อมูล (Excel → Dashboard) — ขั้นตอนจริงที่เคยทำสำเร็จ

✅ **ตอนนี้มี script สำเร็จรูปแล้ว: `update_data.py`** (อยู่ใน `/mnt/user-data/outputs/` ของทุก session ที่เคยรันงานนี้ — ถ้าไม่มีให้สร้างใหม่ตาม logic ในไฟล์นี้ทั้งหมด) รวบรวม mapping + fix ทุกจุดที่เคยแก้ไว้แล้ว (coalesce update, safe_start_con, safe_yyyymmdd_or_text, sheet_status แบบไดนามิก, ฯลฯ) **ทดสอบแล้วให้ผลตรงกับ index.html ปัจจุบัน 100% ทุกฟิลด์**

**วิธีใช้:** แก้ตัวแปร `EXCEL_FILE` และ `REF_DATE_STR` ในไฟล์ (ต้องเช็ค `MAX_STATUS_DATE` ล่าสุดใน AF sheet ก่อนเสมอ ไม่ใช่วันที่ upload) แล้วรัน `python3 update_data.py` — จะได้ `AF_new.json`/`NA_new.json`/`MN_new.json`/`ZM_new.json`/`RJ_new.json` พร้อมรายงานสรุปมาตรฐาน (ตรงกับหัวข้อ 9.1) พิมพ์ออกมาให้ตรวจสอบก่อนเขียนทับไฟล์จริง (สคริปต์ไม่ auto-patch `index.html` ให้ตั้งใจ เพื่อให้มีจังหวะตรวจสอบก่อนเสมอ)

⚠️ **แม้มี script แล้วก็ต้องตรวจสอบโครงสร้าง sheet ใหม่ทุกรอบ** (ชื่อคอลัมน์/ชื่อ sheet/ตำแหน่ง header อาจเปลี่ยนได้แบบไม่แจ้งล่วงหน้า อย่างที่เคยเจอมาแล้วหลายครั้ง — ดูหัวข้อ 8.1.1/8.1.2) ถ้าโครงสร้างเปลี่ยน ต้องแก้ script ให้ตรงก่อนรัน ไม่ใช่รันเชื่อ script เดิมแบบไม่เช็ค

ขั้นตอนด้านล่างคือ logic เดียวกับใน script อธิบายละเอียดเผื่อกรณีไม่มีไฟล์ script (session ใหม่ที่ไม่เห็น output เก่า) ต้องเขียนใหม่จาก README นี้:

### ขั้นตอน
1. **หา header row ของแต่ละ sheet** (ค้นหาด้วยคำ เช่น "รหัสลูกค้า", "ปรับสรุปการUPDATE" เพราะตำแหน่งอาจขยับ): AF (แถวที่ 3), สำหรับกรอง-ตรวจสอบ (แถวที่ 3), รายเดือน-ชน (แถวที่ 61 — เคยขยับมาแล้ว ต้องค้นหาใหม่ทุกครั้ง)
2. **Reconstruct mapping AF sheet → JSON field** ด้วยการเทียบ match rate กับข้อมูลเก่า (ใช้ cust ร่วมกัน) — ระวังจุดที่เคยพลาด:
   - `CONTRACT_DATE` เก็บปีเป็น **พ.ศ. ตรงๆ** (เช่น 2569) แต่ `DATE_01-11` เก็บเป็น **ค.ศ. ปกติ** (2026) — คนละ convention กันในคอลัมน์ datetime เดียวกันของชีตเดียวกัน! ใช้ auto-detect: `if (y < 2100) y += 543`
   - `due_last` มาจาก `Dueล่าสุด` (ตัวเลข YYYYMMDD, ค.ศ., ต้อง +543 หลังแปลง)
   - `due_amt`/`debt` ต้อง format comma 2 ตำแหน่งทศนิยม ไม่ใช่แค่ตัวเลขดิบ
   - `days_lag` = `REF_DATE - MAX_STATUS_DATE` (ไม่ใช่คอลัมน์ AGING_XX ตัวใดตัวหนึ่งตรงๆ)
   - `update` field: coalesce 3 ชั้น (ดูหัวข้อ 2) — ถ้าใช้แค่ `CUSTCARE_STATUS_NAME` อย่างเดียวจะได้ค่าว่างสำหรับกลุ่มไม่ดำเนินการ 143 รายการ
3. **สร้าง NA จาก "สำหรับกรอง-ตรวจสอบ" และ MN จาก "รายเดือน-ชน"** ตามหัวข้อ 1.1 (ห้าม filter จาก AF)
4. **Join `contract_type`** ใหม่ทั้ง 3 array ตามหัวข้อ 5
5. **ตรวจสอบบ้านเลขที่เพี้ยนเป็นวันที่** (ปัญหาที่รู้จักแล้ว, Google Sheets แปลงเลขบ้านแบบ `8/2` เป็นวันที่อัตโนมัติตอน paste จาก Excel) — ค้นด้วย pattern `^\d{4}-\d{2}-\d{2}` ในฟิลด์ `house` ทุกครั้ง ถ้าเจอให้แปลงกลับ (ดูตัวอย่างที่เคยแก้: cust 200199759 → "2/8" ยืนยันจาก number_format ในไฟล์ .xlsx ต้นฉบับ) — **ผู้ใช้รับทราบแล้วว่าจะไม่แก้ที่ต้นทาง Google Sheet ให้ dashboard แก้อัตโนมัติทุกรอบแทน**
6. **อัปเดตวันที่ 2 จุด:** header บนสุด `"อัปเดต: DD-MM-YYYY(พ.ศ.) HH:MM น."` และ `const UPD_REF_DATE=new Date(YYYY,M-1,D);`
7. **ตรวจสอบสมมติฐาน "no-op" ของ OVNA ใหม่** (หัวข้อ 4.3) และเช็คว่า NA/MN มี record ที่ progress ไปสถานะจริงกี่รายการ
8. **ห้ามแก้/ลบ** ฟังก์ชันในหัวข้อ 4 โดยไม่จำเป็น
9. **ตรวจสอบก่อนส่งกลับทุกครั้ง:**
   - JS syntax (`node -e "new Function(...)"`)
   - Column alignment: นับ `<th>` vs `<td>` ต่อแถว (ระวัง cell ที่ generate จากฟังก์ชัน เช่น `ckLagTd()` ไม่นับด้วย regex ธรรมดา ต้อง +1 ต่อจุดที่มี)
   - จำลอง filter สำคัญด้วย Python/Node เทียบข้อมูลจริง (pivot รวมคอลัมน์ = ยอดรวม, curStage/ckStatus/statusLagDays ยัง sync ถูกต้อง, join contract_type สำเร็จกี่ %)
   - ค้น `house` ทุก array ด้วย pattern `^\d{4}-\d{2}-\d{2}` (และรูปแบบผิดปกติอื่น เช่นมี `00:00:00` หรือยาวเกิน 15 ตัวอักษร) ทุกครั้งที่อัปเดตข้อมูลใหม่ — แม้รอบล่าสุด (18-08-69) จะไม่เจอเลยก็ตาม ปัญหานี้ขึ้นกับข้อมูลดิบแต่ละรอบ ไม่ใช่โค้ด จึงต้องเช็คซ้ำทุกรอบ
   - **ทดสอบจริงผ่าน jsdom** (จำลอง browser) ไม่ใช่แค่ตรวจ syntax — set filter, เรียก `ren*()`, เช็คค่าที่ render ออกมาจริง เทียบกับคำนวณอิสระด้วย Python/Node

---

## 7.1 ตาราง Mapping เต็ม — Excel column (sheet AF) → JSON field (ใช้ตรงนี้อ้างอิงได้เลย ไม่ต้อง reconstruct ใหม่)

| JSON field | Excel column (sheet AF) | หมายเหตุการแปลง |
|---|---|---|
| `dept` | `DEPT_SHORT_NAME` | ตรงตัว |
| `bp` | `BP_SHORT_NAME` | ตรงตัว |
| `proj` | `PROJ_NAME` | ตรงตัว |
| `house` | `HOUSE_NO_ACT` | ⚠️ เช็ค pattern `^\d{4}-\d{2}-\d{2}` ทุกครั้ง (ดูข้อ 5 ในขั้นตอน) |
| `cust` | `CUST_CODE` | แปลงเป็น string ไม่มีทศนิยม (`str(int(x))`) |
| `cust_name` | `CUSTNAME1DISPLAY` | ตรงตัว |
| `change_status` | `CHANGE_STATUS` | ตรงตัว |
| `status` | `NEXT_CUSTCARE` | ตรงตัว (ไม่ใช่ curStage-grouped) |
| `update` | coalesce 3 ชั้น | 1) `CUSTCARE_STATUS_NAME` ถ้าไม่ว่าง 2) ไม่งั้น lookup `ปรับสรุปการUPDATE` จาก sheet "สำหรับกรอง-ตรวจสอบ" ด้วย `รหัสลูกค้า`=`cust` 3) ไม่งั้น fallback `NEXT_CUSTCARE` |
| `guarantee` | `ST_GUARANTEE` | ตรงตัว |
| `confirm_flag` | `CONFIRM_FLAG` | ตรงตัว |
| `next_custcare` | `NEXT_CUSTCARE` | เหมือน `status` ทุกประการ (ฟิลด์ซ้ำ ไม่ได้ใช้แล้ว) |
| `start_con` | `CONTRACT_DATE` | datetime, ปีเก็บเป็น **พ.ศ.ตรงๆ** → format `DD/MM/YY` ด้วย `year%100` |
| `due_last` | `Dueล่าสุด` | ตัวเลข `YYYYMMDD`, ปีเป็น **ค.ศ.** → `+543` ก่อนแปลง |
| `due_amt` | `เงินDue` | ตัวเลข → format `{:,.2f}` (comma + ทศนิยม 2 ตำแหน่ง) |
| `overdue` | `งวดค้าง` | ตัวเลข → string จำนวนเต็ม |
| `debt` | `หนี้ค้าง` | ตัวเลข → format `{:,.2f}` |
| `days_lag` | คำนวณ = `REF_DATE - MAX_STATUS_DATE` | `MAX_STATUS_DATE` เป็น `YYYYMMDD` ค.ศ. — **ห้ามใช้ column อื่นแทน** (ไม่ใช่ AGING_XX ตัวใดตัวหนึ่ง) |
| `ck_custcare_st` | `CK_CUSTCARE_ST` | ข้อความดิบตรงตัว |
| `ck_custcare_st_grp` | derived จาก `ck_custcare_st` | ตัดวันที่ 8 หลักนำหน้าออก, ว่าง→`"ไม่เคยซ่อม"` |
| `d01`...`d11` | `DATE_01`...`DATE_11` | datetime, ปีเป็น **ค.ศ.** (ต่างจาก `CONTRACT_DATE`!) → auto-detect `if(y<2100) y+=543` |
| `aging09` | `AGING_09` | ตัวเลข → string จำนวนเต็ม (ใช้แสดงผลอย่างเดียว ไม่ผูก filter) |
| `ck_aging09` | `CK_AGING_09` | ข้อความดิบตรงตัว |
| `contract_type` | `CONTRACT_TYPE1` | ตรงตัว — มีเฉพาะใน sheet AF เท่านั้น |

**NA (จาก sheet "สำหรับกรอง-ตรวจสอบ", ใช้เมื่อรหัสลูกค้าไม่พบใน AF เท่านั้น — ถ้าพบให้ copy record จาก AF ทั้งหมดแทน):**

| JSON field | Excel column | 
|---|---|
| `dept`/`bp`/`proj`/`house` | `กอง`/`สำนักงาน`/`โครงการ`/`บ้านเลขที่` |
| `cust_name` | `ชื่อลูกค้า` |
| `change_status` | `CHANGE_STATUS` |
| `confirm_flag` | `CONFIRM_FLAG` |
| `update`/`status`/`next_custcare` | `ปรับสรุปการUPDATE` (ทั้ง 3 ฟิลด์ใช้ค่าเดียวกัน) |
| ฟิลด์อื่นทั้งหมด | เว้นว่าง, `ck_custcare_st_grp='ไม่เคยซ่อม'` |

**MN (จาก sheet "รายเดือน-ชน", ใช้เมื่อรหัสลูกค้าไม่พบใน AF เท่านั้น):** เหมือน NA ทุกประการ แต่ `guarantee` ใส่ค่าคงที่ `'หลังค้ำประกัน'` เสมอ (ไม่มีคอลัมน์นี้ใน sheet)

---

## 8. แท็บ "สคล/สคจ ชี้แจง" — ระบบ Sub-tab 3 มุมมอง (ZM/RJ)

แท็บที่ 3 (เดิมชื่อ "ไม่ดำเนินการ(สคล/สคจ ชี้แจง)" — **เปลี่ยนชื่อปุ่มแท็บหลักเป็น "สคล/สคจ ชี้แจง"** แล้ว ส่วนปุ่ม sub-tab 1 ด้านในยังคงชื่อเดิมไว้) ตอนนี้มี **3 มุมมองย่อย** สลับกันด้วยปุ่ม `.stbtn` (ไม่ใช่แท็บหลักแยกกัน):

| Sub-tab | ตัวแปร JS | Sheet ต้นทาง | Join AF? |
|---|---|---|---|
| 1. ไม่ดำเนินการ(สคล/สคจ ชี้แจง) | `NA` | สำหรับกรอง-ตรวจสอบ | ใช่ (ตามหัวข้อ 1.1 เดิม) |
| 2. ดำเนินการเสร็จสิ้น | `ZM` | ซ่อมเสร็จ (header แถวที่ 4) | ใช่ |
| 3. ประสงค์ขอส่วนลด (ชื่อเดิม "รอจัดจ้าง(ประสงค์ขอส่วนลด)") | `RJ` | **ขอลดค่าซ่อม** (เดิมชื่อ "รอจัดจ้าง" — เปลี่ยนแล้วจริง ณ รอบ 21-08-69, header แถวที่ 4 เหมือนเดิม) | ใช่ |

✅ **sheet ต้นทางเปลี่ยนชื่อเรียบร้อยแล้ว** (รอบ 21-08-69): `sheet_name='รอจัดจ้าง'` → `sheet_name='ขอลดค่าซ่อม'` — ตัวแปร JS ยังคงชื่อ `RJ` เหมือนเดิม (คนละเรื่องกับชื่อ sub-tab ในหน้าเว็บ "ประสงค์ขอส่วนลด") พร้อมพบคอลัมน์ `guarantee` เปลี่ยนชื่อจาก `CONSTRUCTION_AGENCY.1` เป็น `ST_GUARANTEE` ในรอบเดียวกัน (ดูหัวข้อ 8.1) — จำนวนแถวขยับจาก 131 → 179 → คงที่ (ไม่ใช่ error ถ้าตัวเลขต่างจากรอบก่อนหน้าการเปลี่ยนชื่อ)

### 8.1 โครงสร้างข้อมูล ZM/RJ (field ใหม่ ไม่เหมือน AF/NA/MN)
```
dept, bp, proj, house, cust, cust_name,
start_con,      // วันทำสัญญา — ⚠️ รูปแบบดิบไม่คงที่ระหว่าง sheet/รอบข้อมูล ต้องใช้ safe_start_con() เท่านั้น (ดู 8.1.1)
due_first,      // ข้อความสถานะอาคาร ("รอส่งมอบอาคาร2") หรือตัวเลข YYYYMMDD แล้วแต่รอบข้อมูล — ใช้ safe_yyyymmdd_or_text()
due_new,        // ตัวเลข YYYYMMDD (ค.ศ.) ต้อง +543 — yyyymmdd_dmy_be() ตรงๆ (คอลัมน์นี้ไม่เคยสลับ)
change_status,  // ข้อความสถานะอาคาร หรือตัวเลข YYYYMMDD แล้วแต่รอบข้อมูล — ใช้ safe_yyyymmdd_or_text() เช่นกัน
agency,         // CONSTRUCTION_AGENCY เช่น "บค.9 พก.3"
guarantee,      // ⚠️ ชื่อคอลัมน์เปลี่ยนแล้ว: เดิม CONSTRUCTION_AGENCY.1 → ตอนนี้คือ ST_GUARANTEE (ตรงกับชื่อคอลัมน์ guarantee ใน AF sheet เอง) ใช้ตั้งแต่รอบ 21-08-69 (ไฟล์ที่เปลี่ยนชื่อ sheet "รอจัดจ้าง"→"ขอลดค่าซ่อม")
o_letter,       // "ส่งจดหมาย" — คอลัมน์ O ใน sheet
p_extra,        // "แจ้งซ่อมเพิ่มเติม" (ZM) หรือ "ใบตอบรับ" (RJ) — คอลัมน์ P ใน sheet ชื่อหัวคอลัมน์ต่างกัน แต่ field JSON ใช้ p_extra เหมือนกัน
q_appt,         // "นัดรับมอบ" — คอลัมน์ Q
note,           // "หมายเหตุ" — คอลัมน์ R
contract_type,  // join จาก AF ด้วย cust (เหมือน NA/MN)
delivery_status // "ยังไม่ส่งมอบ"/"ส่งมอบแล้ว" — join รหัสลูกค้ากับ AF: พบ=ยังไม่ส่งมอบ, ไม่พบ=ส่งมอบแล้ว (ทั้ง ZM และ RJ ใช้กฎเดียวกัน)
status_lag,     // (ZM เท่านั้น) วันค้างสถานะปัจจุบันจริงใน AF — ดูหัวข้อ 8.5
sheet_status,   // (RJ เท่านั้น) ⚠️ ต้องอ่านจาก CUSTCARE_STATUS_NAME ของ sheet จริงทุกครั้ง ห้าม hardcode (ดู 8.1.2 — เคยพลาดมาแล้ว)
af_status,      // (RJ เท่านั้น) update ปัจจุบันจาก AF — ดูหัวข้อ 8.5
af_status_lag,  // (RJ เท่านั้น) วันค้างของ af_status — ดูหัวข้อ 8.5
```
⚠️ **`o_letter`/`p_extra`/`q_appt` เป็น text อิสระจากสาขา** (เช่น "ครั้งที่ 2", "นัดหมายแล้ว", "ยังไม่ได้รับ") ไม่ใช่ true/false — ค่าว่างเปล่าเป็นเรื่องปกติมาก (ส่วนใหญ่ของข้อมูลจะว่าง) **ห้ามบีบเหลือแค่ 2 หมวด "มี/ไม่มี"** ต้องแสดงค่าจริงทั้งหมด (ดูหัวข้อ 8.2)

#### 8.1.1 `safe_start_con()` — วันทำสัญญามีได้ 2 รูปแบบดิบ ต้องเช็คทั้งคู่
เจอจริง: sheet "ซ่อมเสร็จ" คอลัมน์ "วันที่ทำสัญญา" เป็น **Excel serial number ดิบ** (`int64`, เช่น `244459`) แต่ sheet "ขอลดค่าซ่อม" คอลัมน์เดียวกันดันเป็น **datetime object ที่ parse มาแล้ว** (`datetime64[us]`) — ใช้ `pd.Timedelta(days=...)` บวกตรงๆ จะ **overflow เงียบๆ** (ค่าที่ได้ = ว่างเปล่า ไม่ error ชัดเจน) เพราะตัวเลข serial ระดับ 244,000+ วัน คูณ nanosecond ต่อวันแล้วเกิน int64
```python
def safe_start_con(v):
    if pd.isna(v): return ''
    if isinstance(v, (pd.Timestamp, datetime.datetime, datetime.date)):
        return dt_dmy_be(v)          # เป็น datetime อยู่แล้ว ใช้ตรงๆ
    try:
        dt = pd.to_datetime(v, origin='1899-12-30', unit='D')   # ต้องใช้ to_datetime ไม่ใช่ Timedelta บวกเอง
        return dt_dmy_be(dt)
    except Exception:
        return ''
```
**ต้องเช็ค `dtype` ของคอลัมน์นี้ทุกรอบอัปเดต** ทั้ง 2 sheet (`df['วันที่ทำสัญญา'].dtype`) ก่อนสมมติว่าเหมือนรอบก่อน

#### 8.1.2 `sheet_status` (RJ) — ห้าม hardcode ค่าคงที่เด็ดขาด
เคยพลาดมาแล้ว 1 ครั้ง: hardcode `rec['sheet_status'] = 'รอการจัดจ้าง'` เพราะตอนใช้ sheet "รอจัดจ้าง" ค่าคงที่จริงคือ "รอการจัดจ้าง" — พอ sheet เปลี่ยนชื่อเป็น "ขอลดค่าซ่อม" ค่าคงที่จริงกลายเป็น **"อนุมัติสรุปค่าก่อสร้าง"** (คนละสถานะ, คนละความหมาย) ทำให้ตอนเทียบกับ `af_status` (จาก AF) ได้ผลว่า "เปลี่ยนสถานะไปแล้ว 179/179" ทั้งที่จริง 0 รายการ — **badge สีในตารางก็เคย hardcode เทียบกับ string ตายตัวเหมือนกัน ต้องแก้คู่กัน:**
```python
# ✅ ถูกต้อง: อ่านค่าจริงจาก sheet ทุกครั้ง
rec['sheet_status'] = s(r.get('CUSTCARE_STATUS_NAME'))
```
```js
// ✅ ถูกต้อง: เทียบกับ x.sheet_status แบบไดนามิก ไม่ hardcode string
<span class="badge ${x.af_status && x.af_status!==x.sheet_status?'bgr':'bgy'}">
```
**หลักการทั่วไป:** ค่าคงที่ที่ "ดูเหมือนจะไม่เปลี่ยน" จาก sheet ใดๆ (เช่น CUSTCARE_STATUS_NAME ของ ซ่อมเสร็จ/ขอลดค่าซ่อม) **ต้องอ่านจากคอลัมน์จริงเสมอ ห้าม hardcode เป็น string ในโค้ด** แม้จะเช็คแล้วว่าตอนนี้คงที่จริง เพราะ sheet อาจเปลี่ยนชื่อ/ค่าฐานได้ในอนาคตโดยไม่แจ้งล่วงหน้า

### 8.2 UI ของแต่ละ sub-tab (เหมือนแท็บอื่นทุกอย่าง)
filter row + chip + KPI card (แยกตามกอง) + **donut 4 วงเรียงแถวเดียว** (`.fourcol`) + ตาราง pivot สรุป (คอลัมน์ = donut แต่ละวง) + ตารางรายละเอียด + export PDF/Excel

**Donut 4 วง:**
- ZM: สถานะส่งมอบ (join AF) → ส่งจดหมาย → แจ้งซ่อมเพิ่มเติม → นัดรับมอบ
- RJ: สถานะส่งมอบ (join AF) → ส่งจดหมาย → ใบตอบรับ → นัดรับมอบ
- ทั้งคู่ใช้ฟังก์ชัน `multiDonut(key,canvasId,legId,arr,field,emptyLabel)` — แสดง**ค่าจริงทุกค่าที่พบ**แยกสี ค่าว่างจัดเป็นหมวด `'ยังไม่ดำเนินการ'` สีเทาคงที่ (`#CBD5E1`) เสมอ ไม่ใช้ `statusDonut()` (ฟังก์ชันเก่าที่บีบเหลือ 2 หมวด ยังอยู่ในไฟล์แต่เลิกใช้กับ 6 จุดนี้แล้ว)
- donut "สถานะส่งมอบ" (วงแรกของทั้ง ZM และ RJ) สร้างแบบ inline ไม่ผ่าน `multiDonut()`/`statusDonut()` เพราะมีแค่ 2 ค่าคงที่ตายตัวอยู่แล้ว

**ตาราง pivot สรุป:** แต่ละคอลัมน์ = ตัวเลขที่ป้อนเข้า donut วงนั้นพอดี **รวมเฉพาะแนวคอลัมน์ ไม่รวมข้ามคอลัมน์แนวนอน** (เพราะแต่ละคอลัมน์เป็นมิติอิสระกัน ไม่ใช่กลุ่ม exclusive แบบ 11 สถานะ) — ยืนยันด้วยการทดสอบว่าตัวเลขแถว "ผลรวมทั้งหมด" ของแต่ละคอลัมน์ต้องตรงกับตัวเลขใน donut วงนั้นเป๊ะ

### 8.3 ⚠️ บั๊ก Chart.js ที่เจอ — กราฟสร้างตอน container ซ่อนอยู่
ตอนโหลดหน้าครั้งแรก `renZM()`/`renRJ()` ถูกเรียกไว้ล่วงหน้าทั้งคู่ แต่ sub-tab 2/3 ยังมี `display:none` อยู่ (`.na-subview` ไม่มี class `.on`) → Chart.js วัดขนาด canvas ได้ 0×0 ตอนสร้างครั้งแรก แล้วไม่ auto-resize เองแม้สลับแท็บมาดูทีหลัง

**แก้ที่ `naSub(n,btn)`:** ให้เรียก `renZM()`/`renRJ()` ซ้ำทันทีหลังเพิ่ม class `.on` (ตอนที่ container มองเห็นแล้วจริงๆ) — ทุกครั้งที่คลิกเข้า sub-tab กราฟจะถูกสร้างใหม่ด้วยขนาดถูกต้องเสมอ **ต้องคงบรรทัดนี้ไว้เสมอเวลาเพิ่ม sub-tab ใหม่ในอนาคต**

### 8.4 Workflow อัปเดตข้อมูล — เพิ่มจากหัวข้อ 7 เดิม
ทุกครั้งที่ AF เปลี่ยน (ไม่ใช่แค่ AF/NA/MN):
1. **`contract_type` ของ ZM/RJ** ต้อง join จาก AF ใหม่ (มีแค่ใน sheet AF)
2. **`delivery_status` ของ ZM/RJ** ต้อง join รหัสลูกค้า → AF ใหม่ทุกครั้ง (พบ="ยังไม่ส่งมอบ", ไม่พบ="ส่งมอบแล้ว") — ลืมจุดนี้แล้ว donut "สถานะส่งมอบ" ทั้ง 2 sub-tab จะผิดทันที
3. **ตัว ZM/RJ เอง** ก็ต้องอ่านใหม่จาก sheet "ซ่อมเสร็จ"/"รอจัดจ้าง" ทุกรอบเช่นกัน (header แถวที่ 4 เสมอ ยืนยันแล้ว 2 รอบติด)
4. เช็คบ้านเลขที่เพี้ยนใน ZM/RJ ด้วย (pattern เดียวกับหัวข้อ 7 ข้อ 5)
5. **`status_lag` (ZM) และ `af_status`/`af_status_lag` (RJ)** ต้อง join จาก AF ใหม่ทุกครั้งเช่นกัน (ดูหัวข้อ 8.5 ว่าทำไมสำคัญ)
6. **`note` ของ AF** ต้อง join รหัสลูกค้า → sheet "สำหรับกรอง-ตรวจสอบ" ใหม่ทุกครั้ง (คอลัมน์ท้ายสุดของ sheet) — join เข้า AF โดยตรง ไม่ใช่แค่ NA เพื่อให้แท็บภาพรวม+ไม่ดำเนินการภาพรวมได้คอลัมน์ "หมายเหตุ" ด้วยอัตโนมัติ (OVNA derive จาก AF อยู่แล้ว)

### 8.5 คอลัมน์ตรวจจับความเปลี่ยนแปลง (`status_lag` / `af_status` / `af_status_lag`) — ทำไมสำคัญ

**บริบท:** AF เดินหน้าตามความจริงทุกรอบที่อัปเดต แต่ ZM/RJ เป็นแค่ **snapshot ณ ตอนที่ดึงจาก sheet "ซ่อมเสร็จ"/"รอจัดจ้าง"** — ไม่ได้ sync กับ AF อัตโนมัติ (คนละกลไกกับ NA ในหัวข้อ 9 แต่ปัญหาลักษณะเดียวกัน) พอเวลาผ่านไป AF เปลี่ยนแต่ตัว ZM/RJ (คอลัมน์ดิบจาก sheet เช่น dept/bp/proj/house/o_letter/p_extra/q_appt) ยังเป็นชุดเดิม

**คอลัมน์เหล่านี้เพิ่มมาเพื่อ "จับสัญญาณ" ว่า AF ขยับไปจากตอน snapshot หรือยัง โดยไม่ต้องรอสาขาส่ง sheet ชุดใหม่มาเทียบเอง:**

| Sub-tab | คอลัมน์ | ที่มา | สัญญาณที่บ่งบอกความเปลี่ยนแปลง |
|---|---|---|---|
| ดำเนินการเสร็จสิ้น (ZM) | `status_lag` ("วันค้างสถานะเสร็จสิ้น") | join AF, คำนวณวันค้างของ**สถานะปัจจุบันจริงใน AF** (ไม่ใช่ยึดติด "ดำเนินการเสร็จสิ้น" ตายตัว) | เลขสูงขึ้นเรื่อยๆ = ส่งมอบล่าช้า / คอลัมน์ `delivery_status` เปลี่ยนเป็น "ส่งมอบแล้ว" = หลุดจาก AF แล้ว (ส่งมอบสำเร็จ) |
| รอจัดจ้าง (RJ) | `sheet_status` | ค่าดิบจาก sheet "รอจัดจ้าง" เอง (คงที่ "รอการจัดจ้าง" เสมอ ไม่เปลี่ยนตาม AF) | ใช้เป็น baseline เทียบกับ `af_status` |
| รอจัดจ้าง (RJ) | `af_status` ("CUSTCARE_STATUS_NAME update จาก AF") | join AF, ดึง `update` ปัจจุบัน | ถ้า ≠ "รอการจัดจ้าง" = ลูกค้าตัดสินใจแล้ว (ขอลดค่าซ่อม/ซ่อมต่อ → AF ขยับสถานะจริงไปแล้ว) — badge เปลี่ยนจากเหลืองเป็นเขียวอัตโนมัติในโค้ด |
| รอจัดจ้าง (RJ) | `af_status_lag` | join AF, คำนวณวันค้างของ `af_status` | ใช้ดูว่าค้างสถานะใหม่นั้นมานานแค่ไหนแล้ว |

**สูตรคำนวณ `*_lag` ทั้งหมด** ใช้ logic เดียวกับ `statusLagDays()` (หัวข้อ 4) แต่คำนวณที่ฝั่ง Python ตอน build ข้อมูล (ไม่ใช่ JS runtime) เพราะ ZM/RJ ไม่ได้เก็บฟิลด์ d01-d11 เต็มแบบ AF — ต้อง reimplement logic เดียวกัน (parse_dmy + stageIdx + REF_DATE) ในสคริปต์ Python ตอน join ทุกครั้ง (ดูตัวอย่างสคริปต์ที่เคยใช้ได้จริงในประวัติการแก้ไขข้อ 26)

⚠️ **ข้อควรระวังตอนเขียนสคริปต์ Python สำหรับ join นี้:** อย่าเปิดไฟล์ `index.html` ด้วย `open(..., 'w')` ทิ้งไว้เฉยๆ ก่อนเขียนเนื้อหาจริง — จะเป็นการ truncate ไฟล์ทันทีตอนเปิด (เคยพลาดมาแล้วครั้งหนึ่ง กู้คืนจากไฟล์ที่เพิ่ง `present_files` ไปล่าสุดได้ทัน) ให้คำนวณ/เตรียมข้อมูลให้เสร็จก่อน แล้วค่อยเปิดเขียนเป็นขั้นตอนสุดท้ายเท่านั้น

---

## 9. วงจรข้อมูล — ทำไมตัวเลขระหว่าง AF กับ สคล/สคจ ชี้แจง ไม่เท่ากันบางรอบ

`NA` (สคล/สคจ ชี้แจง) เป็น list ที่**สาขากรอกเองแยกต่างหากจาก AF** ไม่ได้ sync อัตโนมัติ จึงมีวงจรตามธรรมชาติ:

1. **ส่งข้อมูลให้สาขาชี้แจงรอบใหม่** → list ถูกรีเฟรชให้ตรงกับ AF ปัจจุบัน → **เท่ากัน** (ตัวอย่างจริง: 18-08-69 รีเฟรชแล้ว NA=143=จำนวนที่ update นอก 11 สถานะใน AF พอดี, มี record สถานะจริงปน 0 รายการ)
2. **ระหว่างรอบ** AF เดินหน้าไปเรื่อยๆ ตามความจริง แต่ list ที่สาขากรอกยังค้างที่เดิม → **เริ่มไม่เท่ากัน** (มีคนที่ progress ไปสถานะจริงแล้วแต่ยังค้างอยู่ใน list เก่า — เคยเจอสูงสุด 40/143 รายการ)
3. **รอบถัดไปที่ส่งให้สาขาชี้แจงใหม่** → list ถูกรีเฟรชอีกครั้ง → **กลับมาเท่ากัน** (ตัวอย่างจริง: 21-08-69 รีเฟรช NA จาก 163→98 แถว, record สถานะจริงปนจาก 40→0)

**ไม่ใช่บั๊ก** — เป็นธรรมชาติของ workflow ที่มีคนกรอกข้อมูลสองฝั่งไม่พร้อมกัน อย่าตกใจถ้าตัวเลขไม่นิ่ง ให้เช็คแทนว่า ณ ตอนนี้กำลังอยู่ช่วงไหนของวงจร (ดูหัวข้อ 4.3 วิธีเช็คว่ามี record สถานะจริงปนอยู่กี่รายการ)

### 9.1 รายงานสรุปมาตรฐาน — ใช้ทุกครั้งที่อัปเดตข้อมูล

⚠️ **ห้าม automate ทั้ง workflow แบบไม่มีคนเช็ค** — ข้อมูลมาจาก 3 จุดเวลาต่างกัน (Oracle export → ส่งสาขาตรวจสอบ → Google Sheet) ทำให้แต่ละรอบอาจอยู่คนละช่วงของวงจร (หัวข้อ 9) รันสคริปต์ตรงๆ โดยไม่รายงานให้ผู้ใช้เช็คก่อนเขียนกลับไฟล์จริง มีความเสี่ยงพลาดสูง

ทุกรอบอัปเดตข้อมูล **ต้องรายงานสรุปรูปแบบนี้ให้ผู้ใช้ตรวจสอบก่อนส่งไฟล์**:

```
📊 สรุปการอัปเดตข้อมูล [วันที่]

จำนวนแถวต้นทาง (Excel):
  AF: xxx | สำหรับกรอง-ตรวจสอบ: xxx | รายเดือน-ชน: xxx | ซ่อมเสร็จ: xxx | รอจัดจ้าง: xxx

จำนวนใน Dashboard (เทียบเก่า→ใหม่):
  ภาพรวม (AF):        xxx → xxx
  ไม่ดำเนินการภาพรวม:   xxx → xxx
  สคล/สคจ ชี้แจง (NA):  xxx → xxx  ⚠️ (ถ้าต่างจาก "สำหรับกรอง-ตรวจสอบ" จะเตือน)
  ข้อมูลหน่วยงานอื่น (MN): xxx → xxx
  ดำเนินการเสร็จสิ้น (ZM): xxx → xxx
  รอจัดจ้าง (RJ):       xxx → xxx

เช็คความถูกต้อง:
  ✅/❌ NA มี record สถานะจริงปนอยู่ กี่ % (บอกว่าอยู่ช่วงไหนของวงจร)
  ✅/❌ MN มี record สถานะจริงปนอยู่ กี่ %
  ✅/❌ บ้านเลขที่เพี้ยนเป็นวันที่ (0 = ผ่าน)
  ✅/❌ Join contract_type/delivery_status สำเร็จกี่ %
  ✅/❌ Column alignment ทุกตาราง
```

**หลักการ:** รายงานตัวเลข**ก่อน**เขียนทับไฟล์จริงเสมอ ให้ผู้ใช้เห็นว่า "ถูกหรือผิดตามที่คาดไว้" ได้ทันทีจากตัวเลขเทียบเก่า→ใหม่ — ถ้าตัวเลขไหนดูผิดปกติ (เช่น สคล/สคจ ต่างจาก sheet ต้นทาง, หรือ join ได้ต่ำกว่าที่ควร) ให้หยุดถามก่อน ไม่ใช่เขียนทับแล้วค่อยแก้ทีหลัง

---

## 10. ประวัติการแก้ไข (สรุปเรียงตามลำดับ)

1. ลบ filter "สถานะซ่อมถัดไป" (ov-next) — dead code
2. เพิ่ม "ไม่ดำเนินการ" เป็นตัวเลือกที่ 12 ใน filter สถานะซ่อมปัจจุบัน (`curStage`, `STAGES12`)
3. แก้ตาราง Pivot ให้รองรับ 12 คอลัมน์ ผลรวมตรงกับยอดรวมเสมอ
4. เพิ่ม `dCell()` ซ่อนคอลัมน์วันที่ (d01-d11) ที่ล้ำหน้าสถานะปัจจุบัน
5. เพิ่ม `ckStatus()` sync "เช็คสถานะซ่อมหลังสุด" ให้ตรงกับสถานะปัจจุบัน
6. เพิ่มคอลัมน์ "รหัสลูกค้า" + "ประเภทสัญญา" ทุกแท็บ (join จาก AF sheet)
7. เพิ่ม filter "ประเภทสัญญา" ทุกแท็บ
8. เพิ่มระบบ filter chip (แสดง/ลบ filter ที่เลือกอยู่) ทุกแท็บ
9. เพิ่มคอลัมน์+filter "ปรับสรุปการ UPDATE" เฉพาะแท็บภาพรวม
10. เพิ่มคอลัมน์+filter "วันค้างทำสัญญา" ทุกแท็บ พร้อมแก้บั๊ก null ปนเข้า bucket 0-30 วัน (`inLagRangeStrict`)
11. แก้บ้านเลขที่เพี้ยนเป็นวันที่ (Google Sheets auto-convert) 2 รายการ
12. **อัปเดตข้อมูลรอบแรกจาก Excel ใหม่** (18-08-69) — reconstruct mapping ทั้งหมด, พบบั๊กปีพ.ศ./ค.ศ.ปนกันในคอลัมน์ datetime, พบ logic coalesce ของ `update` field
13. **แก้ NA ให้ยึดฐานจาก sheet "สำหรับกรอง-ตรวจสอบ" แทนการ filter จาก AF** (163 แถวเสมอ ไม่ใช่ 143) — ตามด้วย MN ที่ใช้หลักการเดียวกันอยู่แล้ว
14. **พบบั๊ก "วันค้างสถานะ" (days_lag)** ล้ำหน้าสถานะจริงได้ถึง 100+ วัน แก้ด้วย `statusLagDays()`
15. **พบว่า NA/MN ไม่ใช่ no-op อีกต่อไป** (มี record progress ไปสถานะจริงปนอยู่ 39 และ 36 รายการ) — ต้องเอาฟังก์ชันหัวข้อ 4 ไปใช้กับ NA/MN ด้วย ไม่ใช่แค่ภาพรวม
16. **พบบั๊ก "วันค้างสถานะซ่อมหลังสุด"** ซ่อนอยู่ใน `ckLagValue()` (fallback ไปใช้ `days_lag` ดิบตัวเดียวกับข้อ 14) แก้ให้ fallback ไป `statusLagDays()` แทน — เปลี่ยน signature ฟังก์ชันจากรับ 2 พารามิเตอร์เป็นรับ record ทั้งก้อน
17. **แก้ปัญหา Export Excel เพี้ยนซ้ำ** — `XLSX.utils.table_to_sheet()` มี auto-detect วันที่/ตัวเลขเหมือน Excel/Google Sheets เอง เสี่ยงแปลงเลขบ้านที่ดูเหมือนวันที่ (เช่น "2/8") กลับเป็นวันที่ตอน export ซ้ำอีกรอบ แก้ด้วยเพิ่ม `{raw:true}` บังคับอ่านทุกเซลล์เป็นข้อความดิบเสมอ (ผลข้างเคียง: คอลัมน์ตัวเลข/เงินใน Excel ที่ export จะเป็นข้อความ ไม่ใช่ตัวเลขที่ SUM ต่อได้ทันที)
18. **อัปเดตข้อมูลรอบสอง** (21-08-69) — AF 1,002 / NA รีเฟรชจาก 163→98 (ตรงกับ "สำหรับกรอง-ตรวจสอบ" sheet ล่าสุด) / MN 151
19. **เพิ่มระบบ sub-tab 3 มุมมองในแท็บ "สคล/สคจ ชี้แจง"** — ไม่ดำเนินการ(เดิม) + ดำเนินการเสร็จสิ้น (ZM, จาก sheet "ซ่อมเสร็จ") + รอจัดจ้าง(ประสงค์ขอส่วนลด) (RJ, จาก sheet "รอจัดจ้าง") — ดูรายละเอียดหัวข้อ 8
20. ลองใช้ Ranking bar chart ตามสาขาก่อน → พบบั๊ก Chart.js สร้างตอน container ซ่อน (หัวข้อ 8.3) → แก้แล้วเปลี่ยนใจเป็น donut 4 วงแทนตามคำแนะนำผู้ใช้ (ดูง่ายกว่า ไม่ซ้ำซ้อนกับ pivot table)
21. เพิ่ม `delivery_status` ให้ RJ ด้วย (เดิมมีแค่ ZM) — ถอด donut "ตามกอง" ของ RJ ออก เปลี่ยนเป็น donut "สถานะส่งมอบ" แบบเดียวกับ ZM
22. เปลี่ยนชื่อปุ่มแท็บหลักจาก "ไม่ดำเนินการ(สคล/สคจ ชี้แจง)" → "สคล/สคจ ชี้แจง"
23. **เปลี่ยน `statusDonut()` เป็น `multiDonut()`** สำหรับ 6 donut คอลัมน์ติดตามงาน — แสดงค่าจริงทั้งหมดแทนการบีบเหลือ 2 หมวด "มี/ไม่มี"
24. แก้สีหัวคอลัมน์ตาราง pivot ของ ZM/RJ ให้มีพื้นหลังสี (เดิมใส่แค่สีตัวอักษร ลืม background) ให้ตรงกับแท็บอื่น + แก้คอลัมน์ "หมายเหตุ" ในตารางรายละเอียดให้ขึ้นบรรทัดใหม่และชิดบน (`vertical-align:top`)
25. **กำหนดรายงานสรุปมาตรฐาน (หัวข้อ 9.1)** สำหรับทุกครั้งที่อัปเดตข้อมูล — เพราะ workflow มี 3 จุดเวลาต่างกัน (Oracle → สาขาตรวจสอบ → Google Sheet) จึง automate เต็มรูปแบบไม่ได้ ต้องรายงานตัวเลขให้ผู้ใช้เช็คก่อนเขียนทับไฟล์จริงเสมอ
26. **เพิ่มคอลัมน์ตรวจจับความเปลี่ยนแปลง (หัวข้อ 8.5)** วางหลัง "การค้ำประกัน" ทั้ง ZM (`status_lag`) และ RJ (`sheet_status`/`af_status`/`af_status_lag`) — join AF ใหม่ทุกครั้ง คำนวณวันค้างด้วย logic เดียวกับ `statusLagDays()` แต่ทำที่ฝั่ง Python ตอน build (ระหว่างทำพลาดเปิดไฟล์ index.html ด้วย `open(...,'w')` ทิ้งไว้เฉยๆ ทำให้ไฟล์ถูก truncate ชั่วขณะ กู้คืนจากไฟล์ที่ present_files ไปล่าสุดได้ทัน — บันทึกเป็นข้อควรระวังไว้ในหัวข้อ 8.5 แล้ว)
27. อธิบาย use case ของคอลัมน์ข้อ 26 ชัดเจน — ใช้จับสัญญาณ "ส่งมอบล่าช้า" (ZM) และ "ลูกค้าตัดสินใจแล้ว ขอลดค่าซ่อม/ซ่อมต่อ" (RJ) โดยไม่ต้องรอสาขาส่ง sheet ชุดใหม่มาเทียบเอง
28. **อัปเดตข้อมูลรอบ 3** (25-08-69, ข้อมูลจริงยังเหมือนรอบก่อนทุกอย่างเพราะเป็นชุดทดสอบ) — พบ sheet "ซ่อมเสร็จ"/"รอจัดจ้าง" คอลัมน์ "วันที่นัดส่งมอบครั้งแรก" กับ "CHANGE_STATUS" สลับบทบาทกัน (วันที่นัดฯ กลายเป็นตัวเลข, CHANGE_STATUS กลายเป็นข้อความ) — เขียนฟังก์ชัน `safe_yyyymmdd_or_text()` รองรับทั้ง 2 กรณีอัตโนมัติกันสลับอีกในอนาคต
29. **เพิ่มคอลัมน์ + filter "หมายเหตุ"** ในแท็บ "ไม่ดำเนินการ(สคล/สคจ ชี้แจง)" — join จาก sheet "สำหรับกรอง-ตรวจสอบ" คอลัมน์ใหม่ท้ายสุด, filter แบ่ง 2 กลุ่ม (มีหมายเหตุ/ไม่มีหมายเหตุ) ใช้ `chip-blue` (สีเดียวกับประเภทสัญญา เพราะเป็นข้อมูลเชิงเนื้อหา ไม่ใช่วันค้าง/สถานะ)
30. เปลี่ยนชื่อ sub-tab "รอจัดจ้าง(ประสงค์ขอส่วนลด)" → **"ประสงค์ขอส่วนลด"** (ปุ่ม sub-tab, หัวข้อการ์ด, ชื่อไฟล์ export) — sheet ต้นทาง "รอจัดจ้าง" แจ้งไว้ล่วงหน้าว่าจะเปลี่ยนชื่อเป็น **"ขอลดค่าซ่อม"** ในไฟล์ถัดไป (ยังไม่ได้รับไฟล์ ณ ตอนบันทึก — ดูหมายเหตุหัวข้อ 8)
31. **รับไฟล์ที่เปลี่ยนชื่อ sheet "รอจัดจ้าง"→"ขอลดค่าซ่อม" จริง** (รอบ 21-08-69) — เจอ 3 ปัญหาใหม่พร้อมกัน: (1) คอลัมน์ `guarantee` เปลี่ยนชื่อเป็น `ST_GUARANTEE` ทั้ง ZM/RJ (2) `pd.Timedelta` overflow กับ "วันที่ทำสัญญา" ที่เป็น serial number ใหญ่ (ต้องใช้ `pd.to_datetime` แทน — ดู 8.1.1) (3) **บั๊กจากตัวเอง** hardcode `sheet_status='รอการจัดจ้าง'` ตายตัว ทำให้ตอน sheet เปลี่ยนค่าคงที่จริงเป็น "อนุมัติสรุปค่าก่อสร้าง" การเทียบ badge เพี้ยนหมด (ดู 8.1.2 — บทเรียนสำคัญ: **ห้าม hardcode ค่าคงที่จาก sheet แม้เช็คแล้วว่าตอนนี้คงที่จริง**) จำนวนแถว RJ ขยับ 131→179
32. **อัปเดตข้อมูลรอบ 4** (25-08-69) — **ข้อมูลจริงเปลี่ยนแปลงชัดเจนครั้งแรก** (ก่อนหน้านี้เป็นชุดทดสอบซ้ำๆ): AF 1,002→986 (32 คนหลุด/16 คนใหม่/80 คนขยับสถานะ), NA เริ่มมีสถานะจริงปนอยู่ 19/98 (เข้าสู่วงจร "ไม่เท่ากัน" ตามหัวข้อ 9), ZM มี 14/102 คนส่งมอบสำเร็จแล้ว (`delivery_status` เปลี่ยนเป็น "ส่งมอบแล้ว" ครั้งแรกในข้อมูลจริง)
33. เขียน README หัวข้อ 8.1.1/8.1.2 บันทึกบั๊ก `safe_start_con()`/`sheet_status` แบบละเอียด (ก่อนหน้านี้แก้ในโค้ดแล้วแต่ไม่เคยบันทึกไว้ ต้องตามเก็บย้อนหลัง)
34. **เพิ่มคอลัมน์ "หมายเหตุ" ให้แท็บภาพรวม + ไม่ดำเนินการภาพรวม** — join `note` เข้า `AF` โดยตรง (ไม่ใช่แค่ `NA`) จาก sheet "สำหรับกรอง-ตรวจสอบ" ด้วยรหัสลูกค้า เพราะ OVNA คำนวณจาก `AF.filter(...)` แบบ real-time ใน JS จึงได้คอลัมน์นี้อัตโนมัติโดยไม่ต้อง join ซ้ำ 2 รอบ — วางท้ายสุดของตารางรายละเอียดทั้ง 2 แท็บ (65/986 มีหมายเหตุ ณ ชุดข้อมูลล่าสุด) ยังไม่มี filter (มีแค่คอลัมน์แสดงผล ต่างจาก NA ที่มี filter มี/ไม่มีหมายเหตุด้วย)
35. **สร้าง `update_data.py`** — สคริปต์ Python สำเร็จรูปรวม logic การแปลง Excel→JSON ทั้งหมด (เดิมไม่เคยบันทึกเป็นไฟล์ มีแต่คำสั่งชั่วคราวในแต่ละ session ทำให้ session ใหม่ต้อง reconstruct จาก README ทุกครั้ง) ทดสอบแล้วให้ผลตรงกับ index.html ปัจจุบัน 100% ทุกฟิลด์ (AF/NA/MN/ZM/RJ) — เก็บไว้ที่ `/mnt/user-data/outputs/update_data.py` แก้แค่ `EXCEL_FILE`/`REF_DATE_STR` แล้วรันได้เลย ยังคงต้องตรวจสอบโครงสร้าง sheet ใหม่ทุกรอบก่อนเชื่อ script เดิม
36. **อัปเดตข้อมูลรอบใหม่ (25-08-69, ผ่าน `update_data.py`)** — ทดสอบ script ข้อ 35 กับไฟล์ Excel จริงรอบใหม่ครั้งแรก: AF/NA/MN/ZM/RJ นับแถวตรงกับรอบก่อนหน้าทุกตัว (986/98/151/102/179, ไม่มีคนเข้า/ออกระบบ) มีแค่ 7 คนที่ `update` ขยับ (สคล.สาขาออเงิน, "ยังไม่สำรวจ"→ progress) ยืนยันว่า script ใช้ซ้ำได้จริงข้าม session — จากนั้น **เพิ่ม filter "หมายเหตุ" (มี/ไม่มีหมายเหตุ)** ในแท็บภาพรวม และ ไม่ดำเนินการภาพรวม (ต่อจากคอลัมน์แสดงผลอย่างเดียวในข้อ 34) ใช้ field `note` ที่ join ไว้แล้ว ไม่ต้องแก้ `update_data.py` เพราะเป็นแค่ frontend filter logic ใน `index.html`

**ตัวเลขอ้างอิงล่าสุด (25-08-69):** AF 986 (มีหมายเหตุ 65) | ไม่ดำเนินการภาพรวม 91 | NA (สคล/สคจ) 98 | ZM (ดำเนินการเสร็จสิ้น) 102 | RJ (ประสงค์ขอส่วนลด) 179 | MN (ข้อมูลหน่วยงานอื่น) 151 — ทุกจุดทดสอบผ่าน jsdom แล้ว
